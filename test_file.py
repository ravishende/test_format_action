if __name__ == "__main__":
    x = 5
    y = x + \
        5
    z = x + y
    sum([x, y,
        z])
    # this is an extra long comment that should hopefully be split across multiple lines if black formatting is correctly applied. If it is not, this comment will stay on one line, despite it being far more than the usual characters per line.

